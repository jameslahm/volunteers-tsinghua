const host = 'https://2019-a16.iterator-traits.com'
const port = '443'
const appid = ''
const appSecret = ''

const config = {
  host,
  appid,
  appSecret,
  DEBUG: false,
  port
}

export default config
